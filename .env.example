# PondMonitor Configuration Example
# Copy this file to .env and customize for your environment
#
# USAGE:
# - For testing without hardware: cp .env.example .env (uses defaults)
# - For production with hardware: customize SERIAL_PORT and TESTING_MODE
# - For Docker deployment: uses service names (timescaledb, redis)
# - For local development: uses localhost

# =============================================================================
# Database Configuration (Required)
# =============================================================================
POSTGRES_USER=pond_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=pond_data
POSTGRES_PORT=5432
PG_HOST=timescaledb

# =============================================================================
# Redis Configuration
# =============================================================================
REDIS_HOST=redis
REDIS_PORT=6379

# =============================================================================
# Serial/LoRa Configuration (Hardware-specific)
# =============================================================================
# Serial port for LoRa communication (Linux: /dev/ttyUSB0, Windows: COM3)
# For testing without hardware, this is ignored when TESTING_MODE=true
SERIAL_PORT=/dev/ttyUSB0
BAUD_RATE=9600

# Testing mode settings (IMPORTANT for testing PC)
# TESTING_MODE=true: Disables hardware communication, generates simulated data
# SIMULATE_DATA=true: Creates realistic sensor data without physical sensors
TESTING_MODE=true
SIMULATE_DATA=true

# Gateway settings
RETRY_DELAY=5
MAX_RETRIES=3

# =============================================================================
# Flask Application Configuration
# =============================================================================
FLASK_PORT=5000
FLASK_ENV=production
FLASK_SECRET_KEY=change_this_in_production

# =============================================================================
# Weather API Configuration (Optional but recommended)
# =============================================================================
# Coordinates for weather data (example: Palkovice, Czech Republic)
# Change these to your location for accurate weather data
WEATHER_LAT=49.6265900
WEATHER_LON=18.3016172
WEATHER_ALT=350

# Cache duration in seconds (3600 = 1 hour) - reduces API calls
WEATHER_CACHE_DURATION=3600

# User agent for API requests (REQUIRED by met.no API)
# Change email to your contact for API compliance
USER_AGENT=PondMonitor/1.0 (your@email.com)

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=INFO

# =============================================================================
# Optional: Monitoring Configuration
# =============================================================================
# Uncomment to enable Grafana monitoring
# GRAFANA_PASSWORD=admin

# =============================================================================
# Development/Testing Overrides
# =============================================================================
# Uncomment these for development/testing
# FLASK_ENV=development
# TESTING_MODE=true
# SIMULATE_DATA=true
# LOG_LEVEL=DEBUG